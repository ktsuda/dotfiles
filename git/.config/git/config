[user]
    name = ktsuda
    email = ktsuda@users.noreply.github.com
[core]
    editor = nvim
    pager = cat
    ignoreCase = false
    excludesFile = ~/.config/git/ignore
    quotepath = false
[merge]
    conflictStyle = diff3
[push]
    default = simple
    autoSetupRemote = true
[diff]
    colorMoved = default
    colorMovedWS = allow-indentation-change
[alias]
    fix = commit --amend
    now = commit --amend --no-edit
    l = log --graph --date-order --abbrev-commit --date=relative --pretty=format:'%C(cyan)%ad %C(magenta)%h %C(green)%cn %C(white)%s %C(yellow)%d%Creset'
    clang-format = clang-format-12
[credential]
    helper = store
[init]
    defaultBranch = main
[clangFormat]
    binary = clang-format-12
    style = file
[ghq]
    root = ~/Library/Mobile Documents/iCloud~md~obsidian
    root = ~/Projects
    root = ~/go/src
    root = ~/src

# vim:set ts=4 sw=4 sts=4 et:
